import pytest

from core.handler import Handler


class TestCoreHandler:
    pass
    # Test that CopyFileHandler can turn relative source into absolute path
    # Test that CopyFileHandler can turn relative destination into absolute path
    # Test that CopyFileHandler will create source path it does not exist
    # Test that CopyFileHandler will create destination path it does not exist
